/// theme module of cc_core: the shared base theme over each app's
/// [CcThemeTokens] — every CC app's screens read as one continuous
/// sheet of paper, in the app's own colors.
library;

export 'cc_theme.dart';
